# sslChecker

Go の学習を兼ねた、複数の SSL 証明書の有効期限をチェックする CLI ツール。

## このプロジェクトでの進め方（最重要）

**コードは書かないでください。** 学習が目的なので、実装は必ずユーザー自身が書きます。

- 実装（関数の中身）を提示しない。書き写せる形のコードを出さない
- 型・シグネチャの「形」、API 名、標準ライブラリの関数名を伝えるのは可
- ユーザーが書いたコードを `go vet` / `go run` で実際に動かして確認し、指摘する
- 指摘は必ず理由とセットで。「なぜそうするか」を Go の文化・慣習とともに説明する
- 答えを先回りして全部言わない。まず自分で書かせ、詰まったらヒントを段階的に出す
- コンパイルエラーは「読み方」から説明する。エラーメッセージを教材として使う

## 学習方針

- pkg.go.dev と `go doc` を自分で引く習慣をつけてもらう
- 口で説明するより、実際にコマンドを動かして見せるほうが理解が早い
- 標準ライブラリのソースを読むことを推奨する（`go doc -src`）
- 寄り道は楽しいが沼るので、まずツールを完成させる方向に戻す

## 環境

- Nix flake + direnv で Go 1.27（`flake.nix` にピン留め済み）
- 確認コマンド: `go vet ./...` と `go run ./cmd/sslchecker`
- モジュール名が `sslCheker`（Checker のタイポ）。直すなら go.mod の1行
- `.gitignore` で `/.go/`（GOPATH。モジュールキャッシュが入る）、`/sslchecker`（ビルド成果物）、
  `.direnv/`（direnv のキャッシュ）を除外済み
- `flake.nix` に `golangci-lint` を追加済み。まだ一度も走らせていない（沼りそうなら後回しでよい）

## 進捗

### 完了

**Step 1: 1ホストの証明書取得と残り日数の計算**
- `fetchCert(host string) (*x509.Certificate, error)` に切り出し済み
- `net.JoinHostPort` でホストとポートを結合（IPv6 対応のため）
- `defer conn.Close()` は err チェックの直後
- `len(peerCerts) == 0` のガードを添字アクセスの前に

**エラー処理**
- `fmt.Errorf` + `%w` でラップ。書式は `fetch cert <host>: <原因>`
- エラーは `os.Stderr` へ、失敗時は `os.Exit(1)`

**Step 2: 残り日数の計算を切り出し**
- `daysUntil(notAfter, now time.Time) int` に切り出し済み
- `time.Now()` は `main` 側で呼び、引数で渡す設計（テストで任意の日付を渡せる）

**テーブル駆動テスト（`cmd/sslchecker/main_test.go`）**
- `TestDaysUntil` が匿名構造体スライス + `t.Run` のサブテスト形式。緑
- `now` は `time.Date(2026, 9, 3, 17, 15, 0, 0, time.UTC)` で固定（UTC 固定は実行環境非依存にするため）
- フィールドは `name` / `d time.Duration` / `want int`。`notAfter` はループ内で `now.Add(tt.d)` で組み立てる
- ケースは6件: 30日 / 30日12時間の端数あり / 残り0日 / 12時間残ってる / 12時間すぎていた / 24時間すぎていた

**Step 3: `daysUntil` の仕様確定と実装修正（PR #3、2026-09-04 完了）**
- **決めた仕様**: 残り日数は常に floor（数直線の左へ）で丸める
- **理由**: 安全側に倒すため。「残り31日」と表示すると利用者が「まだ余裕がある」と誤解する。
  期限・在庫のような「足りないと困るもの」は少なめに見積もる
- **副産物**: `0以上 = まだ有効 / 負 = 失効済み` が符号だけで判定できる。閾値判定を足すときに効く
- 実装を `int(d.Hours()/24)` → `int(math.Floor(d.Hours()/24))` に修正（`math` を import）
- ドキュメントコメントを追加。`go doc -u ./cmd/sslchecker daysUntil` で表示確認済み
- **バグの見つかり方**: ゼロ方向切り捨てと floor がズレるのは「負かつ端数あり」のときだけ。
  6ケース中「12時間すぎていた」1件だけが落ちた。境界値と負値を試さなければ永久に見つからなかった

**Step 4: 複数ホストの逐次チェック（PR #4、2026-09-08 完了）**
- `hosts := []string{...}` を `for _, h := range hosts` で逐次処理
- **設計判断**: `fetchCert` は裸のホスト名（`example.com`）を受け取り、内部で `net.JoinHostPort` する形に。
  理由は「呼び出し側の重複を消す」「`fetchCert` はドメイン寄りの関数なのでこのツールの語彙で受けるべき。
  `host:port` は `tls.Dial` の語彙」。語彙の変換はレイヤーの境界でやる。
  トレードオフ（443 が関数に埋まる）は自覚した上で、必要になるまで作らない
- 変数名も語彙に合わせる。引数は `host`、`JoinHostPort` の結果は `addr`（`tls.Dial` の引数名が `addr`）
- ポートは `fetchCert` 内の `const defaultPort = "443"`。スコープは最小に。
  CLI 化で `--port` を足すときのデフォルト値の置き場所になる
- **エラー時は止めずに続行**。`var failedFetch bool` をループ外に置き、`err != nil` なら
  stderr 出力 → フラグを立てる → `continue`。ループを抜けてから `if failedFetch { os.Exit(1) }`
- `now := time.Now()` はループの外（1回の実行 = 1つの基準時刻）
- 動作確認: 一覧の真ん中に `does-not-exist.example` を混ぜ、前後のホストが両方処理されることを確認済み
- 逐次版の実測: 4ホストで約2.9秒（1ホストあたり 0.7秒程度）

**Step 5: 並行化（2026-09-11）**
- `sync.WaitGroup` で全ホストを並行取得。逐次 2.9秒 → 並行 0.7〜1.4秒（約3.5倍）
- Go 1.25 以降の **`wg.Go(func(){...})`** を使用（`Add(1)` + `defer wg.Done()` を1つにまとめたもの）。
  ネットの記事はほぼ `Add`/`Done` 版なので見比べるとき注意
- **`type result struct{ host string; daysLeft int; err error }`** をパッケージレベルに定義
- goroutine は「計算して `results[i]` に置く」だけ。表示・エラー判定は `wg.Wait()` のあとに
  main goroutine が逐次で回す → **出力順が入力順のまま保たれる**
- `failedFetch` を goroutine から書くのをやめたことで**データ競合が消えた**（Mutex 不要）
- 確認: `go vet` / `go run -race` / `go build` + `time` の3点セット

**この段階の学びの順序（1段ずつ踏んだ）**
1. `go func(){}()` だけ付ける → **何も出力されない**（main が終わるとプログラム終了）
2. `time.Sleep(3*time.Second)` を足す → 出る。ただし逐次より遅い（実作業 0.8秒に3秒待っている）
3. `WaitGroup` に置換 → 「時計を見て止める」から「完了を見て待つ」へ
4. `-race` を走らせる → **失敗ホストが1件だと何も出ない**。2件に増やして初めて検出
5. `results` スライスに添字で書き込む設計へ → 競合が構造的に消える

**やり残し（気が向いたら）**
- コメントに「なぜ小さめに丸めるのか」の理由が入っていない。Copilot の指摘対応で薄まった。
  what はコードで分かるが why はコメントにしか書けない、という話は伝え済み

### 次にやること（ここから再開）

**C-3. 並行数の制限（ここから）**
- いまは全ホストを**無制限に**並行起動している。1万件のリストを渡すと goroutine が1万個立ち、
  ファイルディスクリプタと DNS を食い潰す。実用上ここの制限は必須
- 手段: バッファ付き channel をセマフォとして使う / `golang.org/x/sync/errgroup` の `SetLimit`
  （後者は外部モジュール。`go get` を初めて使う機会になる）
- 制限数を変えて `time` で測ると、速度が頭打ちになる点が見える

**その後**
- CLI 化（`flag`、ファイル/標準入力からホスト一覧、`text/tabwriter`、JSON 出力）
- テスト（`httptest.NewTLSServer`、`x509.CreateCertificate` で期限切れ証明書を自作）
- 発展: 中間証明書の期限、Nagios 互換の終了コード（0=OK/1=WARN/2=CRIT）、`run() error` パターン

## 説明済みの事項（再説明は不要、参照はOK）

- TLS ハンドシェイクと証明書チェーン。`PeerCertificates[0]` がリーフである理由
- `go doc` と pkg.go.dev の使い分け。メソッドは型のセクションの下に並ぶ
- `Close()` と fd リーク。`defer` の位置（err チェックの後）
- `io.Closer` / `net.Conn` / `tls.Conn` の層構造
- `error` はインターフェース。`%w` によるラップ、`errors.Is` / `errors.As`
- `fmt` の Print / Sprint / Fprint / Errorf 一族と接尾辞 f・ln の意味
- stdout と stderr の使い分け、リダイレクト、終了コード（0=成功）
- 命名規則（名前に型を入れない、`Get` を使わない、I/O を伴うなら動詞）
- `time.Time` と `time.Duration`、`Hour()` と `Hours()` の違い、`Days()` が無い理由
- タイムゾーンは表示だけの問題。残り日数の計算には影響しない
- `x509` のフィールド名は RFC 5280 の用語（`NotBefore` / `NotAfter`）
- `go test` はテスト関数を命名規則で発見する（`TestXxx`、`Test` の次の文字は小文字不可）。
  名前が違うと沈黙して `no tests to run` になる。`go test` は裏で `go vet` の一部を実行する
- 未使用でコンパイルエラーになるのはローカル変数と import だけ。パッケージレベルの関数・変数は対象外
- 関数内のローカル変数のスコープは宣言地点から。パッケージレベルの宣言は順序不問（この非対称性）
- 先頭が `0` の数値リテラルは8進数。`09` はエラー、`010` は黙って 8 になる。日時をゼロパディングしない
- `time.Hour`（`Duration` 定数、括弧なし）と `now.Hour()`（メソッド、括弧あり）の違い。
  `Duration` の実体は int64 のナノ秒
- `go doc` の読み方: 宣言が `const`/`var`/`func`/`type` のどれで始まるか。
  関数は戻り値の型の下にグループ化される（`time.Date` が `type Time` の下にいる理由）
- Go の定数は ALL_CAPS にしない。名前からは定数だと判断できない
- 命名は「スコープの広さに名前の長さを比例させる」。標準ライブラリの `d Duration` / `u Time` が実例
- 複合リテラルは最後の要素にも末尾カンマが必要（セミコロン自動挿入の仕様）
- テーブルに入れるのは「ケースごとに結果を変える軸」だけ。`now` のような固定値は関数の先頭へ
- `t.Errorf` は `got, want` の順。`go test -run 'TestXxx/サブテスト名'` で1ケースだけ実行できる
- **型なし定数（untyped constant）**: 文脈で型が決まる。`Duration + 12` の `12` は 12ナノ秒になる
  （`Duration` の1単位がナノ秒だから）。だから単位は `12 * time.Hour` のように定数を掛けて書く。
  逆に `int(-0.5)` はコンパイルエラー（定数変換は情報が失われると拒否）、`30.0` は int に厳密変換
  できるので OK。同じ規則の表と裏
- **Go の float→int 変換はゼロ方向への切り捨て**（truncation toward zero）。正の数では floor と
  一致するが、負の数では ceil と一致する。「切り捨て」という日本語が正の数では一意、負の数では
  2通り（0 に寄せる / 数直線の左へ）に割れることに、負値のテストで初めて気づける
- **緑は正しさの証明ではない**。`+ 12`（12ナノ秒）を「12時間」のつもりで書いたテストが緑になった
  実例あり。新しいケースを足したら `want` をわざと壊して一度落とし、検証が効いていることを確認する
- **テストが落ちたら「実装が悪いのか、テストが悪いのか」を毎回考える**。初心者は実装を疑いがちだが
  テスト側のミス（`want` の直し忘れ等）も同じくらい多い。テーブル駆動は行のコピペで作るので特に
- `t.Log` / `t.Logf` は `go test -v` のときだけ表示される（成功したテストは黙っているべき、という思想）。
  デバッグ出力は消してからコミットする
- **`go doc` は既定で非公開シンボルを表示しない**。小文字始まりの識別子は `-u` を付ける。
  `go doc -u -src` で内部実装まで読める
- **ドキュメントコメントの書式**（Go 1.19 で正式化）: `//` だけの行が段落区切り、`//` の直後にタブを
  打つとコードブロック。連続したタブ行は1つのブロックにまとまる。`//` をタブに置き換えてしまうと
  コメントでなくなり構文エラー（`expected declaration, found ...`）。作法は関数名で書き始める
- **コメントは what より why**。「小さめに丸める」はコードを読めば分かる（`math.Floor` がある）。
  分からないのは「なぜそう決めたか」。理由がないと後から親切に元へ戻される
- **gofmt の演算子スペーシング**: 優先順位の高い演算子ほどスペースを詰める。`a*b + c*d` のように
  書かれるので、括弧を数えなくても計算順序が目に入る
- **AI レビュー（Copilot 等）との付き合い方**: 文章の見た目は直せても、設計判断の記録は守ってくれない。
  指摘を取り込んだあと「伝えたかったことが残っているか」を自分で確認する
- **`Dial` は同名の別物が複数ある**。使っているのは `crypto/tls.Dial`（`tls.` という前置きで決まる）。
  `net.Dial` は `net.Conn`（インターフェース）を返すので `ConnectionState()` は呼べない。
  `tls.Dial` は具体型 `*tls.Conn` を返すから呼べる。`net.Dialer` / `tls.Dialer` は「型」で、
  そちらの `Dial` は型に生えたメソッド（タイムアウト対応で戻ってくる）
- **ドキュメントが薄いときは委譲先を辿る**。`tls.Dial` の説明は「using net.Dial」としか書いていない。
  `addr` が `"host:port"` 形式だと書いてあるのは `net.Dial` のドキュメントのほう
- **`range` は (インデックス, 要素) を返す**。受け取る変数が1つだとインデックスだけが来る
  （`map` ならキー）。要素だけ欲しいときは `for _, h := range hosts`。
  Go 1.22 以降、ループ変数はイテレーションごとに新しい変数になる（並行化で効く）
- **`const` の判断基準は「実行中に変わりうるか」の一問**。変わらないなら `const`。
  Go の `const` は数値・文字列・真偽値・rune のみ（スライス・構造体・関数呼び出しの結果は不可）。
  裏返すと「`const` と書いてあれば実行時に何も起きない」という保証になり、`var` より読み手への情報量が多い。
  型は書かない（型なし定数のまま、使う場所で決まる）。`:=` は必ず変数を作るので `const` にはならない
- **`bool` を `true` と比較しない**。`if failed` / `if !failed` と書く。`failed == true` は
  「真かどうかが真か」と言っているだけで冗長。`==` と `!=` の取り違えで意味が静かに反転する事故も起きる
- **ループの中に「毎回同じ結果になる処理」を置かない**。外に出すかどうかは
  「その値はループのたびに変わるか」の一問で決まる。変わるものは中（`peerCert` / `daysLeft` / `Printf`）、
  変わらないものは外（`now` / `failedFetch`）。今回は正しさの話だが、重い処理なら速度の話にもなる
- **時刻・乱数・環境変数のような外部入力は、できるだけ外側で1回だけ取り込んで内側には値として流す**。
  `daysUntil` に `now` を引数で渡した理由と、`main` のループ外に出した理由は同じ。内側で掴むほど
  テストできない範囲が広がる。閾値判定（Nagios コード）を足すと、時刻のズレが結果を変える
  ——境界にいるホストが一覧の前後どちらにあるかで警告が出たり出なかったりして、再現しない不具合になる
- **stdout / stderr の分離は実際に確かめられる**。`2>/dev/null` で結果だけ、`1>/dev/null` で
  エラーだけが残る。`go run` 実行時に出る `exit status 1` はプログラムの出力ではなく `go run` 自身が
  自分の stderr に書いたもの（`go build` したバイナリを直接叩くと出ない）
- **コンパイラは変数名の綴りを見てくれない**。`defaultePort` / `faiedFetch` のようなタイポは
  `go vet` も通る。名前を書いたら一度読み返す
- **`go vet` が通ることと意図通り動くことは別**。`if failed != true` は vet も通るが、
  1件目の成功で `os.Exit(1)` していた。動かして初めて分かる（`daysUntil` の「緑は正しさの証明ではない」と同じ話）
- **並行（concurrency）と並列（parallelism）は別物**。goroutine は並行のための道具で、
  複数コアで同時に走るかはランタイム次第。このツールが速くなったのは並列化したからではなく
  「相手の返事を待つ時間」を重ねたから。`time` の出力が `2% cpu` なのがその証拠
  （CPU を使ったのは 0.04秒、残り1.4秒は待っているだけ）。コアを増やしても1ミリ秒も速くならない
- **`go` 文は起動するだけで待たない**。`main` goroutine が return した時点でプログラムは終了し、
  他の goroutine は道連れで殺される（言語仕様）。だから `go func(){}()` だけ書くと何も出力されない
- **`time.Sleep` で待つのは「止める」であって「待つ」ではない**。時計しか見ておらず、goroutine の
  完了とは無関係。足りなければ黙って結果が欠け、多すぎれば並行化の効果が消える
  （実測: 実作業 0.8秒に3秒 Sleep して合計3.8秒。逐次版 2.9秒より遅い）
- **`continue` / `break` は関数の境界を越えられない**。`go func(){...}()` の中は新しい関数なので
  `continue is not in a loop` になる。代わりに `return`。逐次の「次のホストへ」と並行の
  「この goroutine を終える」では制御の意味自体が違う（次のホストは別の goroutine がもう走っている）
- **`-race` は「起きた競合」しか報告しない**。実行されなかった経路も、タイミングが噛み合わなかった
  競合も見えない。実例: 失敗ホストが1件だと `failedFetch = true` を書く goroutine が1つしかなく
  無検出。2件に増やして初めて `WARNING: DATA RACE` が出た。
  **`daysUntil` の「緑は正しさの証明ではない」と同じ構図**（今回は「正しい出力が正しい実装の証明ではない」）
- **`-race` の出力の読み方**: `Write at <addr> by goroutine N` が今回の書き込み、
  `Previous write ... by goroutine M` が同期関係のない直前の書き込み。それぞれのスタックトレースと
  `Goroutine N created at:`（起動場所）まで出る
- **競合はロックで守るより、共有しない形に設計を変えるほうが強い**。`results[i]` は要素ごとに
  別のメモリなので、複数 goroutine が同時に書いても競合しない（`make` で長さを確定させておくこと）。
  **`append` は絶対にダメ** —— スライス自身を書き換えるので競合し、順序も実行順になる。
  表示を `wg.Wait()` の後に移すと `failedFetch` を goroutine が触らなくなり、守る対象が消える
- **「実害がなさそうな競合」でも消す**。全 goroutine が同じ `true` を書くので結果は変わらないが、
  Go のメモリモデル上は未定義。コンパイラは「競合はない」前提で最適化する権利を持つ
- **`wg.Wait()` はループの外**。中に置くと「1つ起動して1つ待つ」で完全に逐次になる。
  しかも**出力が入力順に並ぶので一見正しく見える**（本当に並行なら DNS 失敗が最速で先頭に来る）。
  今日3回目の同じパターン（`os.Exit` / `now` / `wg.Wait`）——「それはループのたびにやることか？」
- **型はパッケージレベルに置く**。関数内で定義した型には**メソッドを一生付けられない**（メソッド宣言は
  パッケージレベルのみ）。小文字始まりならパッケージ外から見えないので「スコープは最小に」とも矛盾しない。
  原則は「必要以上に広げない」であって「常に最小」ではない
- **構造体はフィールド単位でなく複合リテラルで丸ごと埋める**。`results[i] = result{host: h, err: err}`。
  入れ忘れに気づきやすく、フィールドが増えたときに埋め忘れを探しやすい。
  フィールド名は省略できるが宣言順に依存するので必ず書く。書かなかったフィールドはゼロ値
- **エラー経路でも `results[i]` に必ず値を入れる**。入れずに `return` するとゼロ値のまま残り、
  表示側からは「成功して残り0日」に見える＝エラーの握りつぶし
- **`:=` の左辺は「名前」でなければならない**。`results[i] := ...` は
  `non-name results[i] on left side of :=`。スライスの要素・構造体のフィールド・ポインタの参照先は
  すでに存在する場所なので `=`
- **`fmt` は `error` を渡すと `Error()` を呼ぶ**（`Formatter` → `error` → `Stringer` の順で特別扱い）。
  `%w` だけは `fmt.Errorf` 専用でラップのための動詞、出力には使えない
- **`F` は Writer の F**。`Println` は stdout 固定で第1引数は「出力する値」、`Fprintln` は第1引数が
  `io.Writer`。書き間違えると `os.Stderr`（`*os.File`）の中身が `&{0x...}` と印字され、
  しかもエラーが stdout に出て分離が壊れる。**`go vet` がこれを名指しで検出する**
  （`fmt.Println does not take io.Writer but has first arg os.Stderr`）——コンパイラは通す
- **ループ変数のシャドーイング**に注意。`for i, result := range results` と書くと型 `result` が
  隠れてその中で型として使えなくなる。短いスコープなら `r`
- **速度計測は `go build` してから `time` を打つ**。`time go run ...` はコンパイル時間が乗る
  （実測で 1.43秒 vs 2.13秒）。`time` の出力は user（自分のコード）/ system（カーネル）/ total（実時間）で、
  `cpu%` は `(user+system)/total`。**1回の計測は揺らぐ**ので最低3回（DNS キャッシュが冷えている
  1回目だけ 2.2秒、2回目以降は 0.7〜0.9秒だった）

## 既知の未対応事項

- タイムアウト未設定。応答しないホストで無限に待つ（`tls.Dialer` + `context`）
- `InsecureSkipVerify` 未使用のため、期限切れ証明書はエラーになり中身を読めない
- ポート 443 決め打ち（`fetchCert` 内の `const defaultPort`）、ホスト一覧も `main` にハードコード
- 終了コード 1 になるのは「証明書を取得できなかった」ときだけ。期限切れ（`daysLeft` が負）でも 0 のまま。
  Nagios 互換の終了コードをやるときの宿題

## ユーザーが後でやりたいと言っていること

- TCP エコーサーバを書く（`net.Listen` + goroutine）。このツール完成後のご褒美
